Place the static hero poster here as:

    hero-poster.jpg

It is shown as the no-WebGL fallback (src/App.tsx) and used as the
social-preview og:image (index.html). Use a high-resolution render of the
brushed-aluminium fork on the cream backdrop, ideally ~1600px wide.
