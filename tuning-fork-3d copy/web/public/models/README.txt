Place the tuning-fork model here as:

    udara_fork.glb

Requirements (per the brief):
  - ~0.19 m tall, Y-up, real-world metres
  - metal surface with KHR_materials_anisotropy + roughness/normal/AO textures
  - Draco-compressed geometry and KTX2/Basis textures are supported
    (the loader in src/Fork.tsx is pre-configured for both)
